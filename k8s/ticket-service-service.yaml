apiVersion: v1
kind: Service
metadata:
  name: ticket-service
  namespace: autoops
spec:
  selector:
    app: ticket-service
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP